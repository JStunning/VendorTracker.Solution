@using VendorTracker.Models;
@{
  Layout = "_Layout";
}

<h2>Vendors</h2>

@if (@Model.Count == 0)
{
  <h4>No Vendors have been added yet!</h4>
} 

@foreach (Vendor vendor in Model)
{
  <h4><a href='/vendors/@vendor.Id'>@vendor.Name</a></h4>
}

<p><a href="/vendors/new">Add a Vendor</a></p>